<!-- inside app/views/welcome/index.html.erb -->  
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "Home", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
          <span class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Multi-Sig Wallets
          </span>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown" id="wallet-menu">
            <% ScrapableAddress.where(is_hot_wallet: false).each do |wallet| %>
              <%= link_to wallet.name, wallet_path(wallet), class: "dropdown-item"  %>
            <% end -%>
          </div>
        </li>
        <li class="nav-item dropdown">
          <span class="nav-link dropdown-toggle" href="#" id="navbarDropdown2" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Hot Wallets
          </span>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown2" id="wallet-menu">
            <% ScrapableAddress.where(is_hot_wallet: true).each do |wallet| %>
              <%= link_to wallet.name, wallet_path(wallet), class: "dropdown-item"  %>
            <% end -%>
          </div>
        </li>
        <li class="nav-item dropdown">
          <%= link_to "Search transactions", search_transactions_path, class: "nav-link" %>
        </li>
      </ul>
    </div>    
  </div>
</nav>
